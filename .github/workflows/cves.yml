name: CVE Tests
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  # This is to check the
  cve:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
